Smart Agricultural Crop Recommendation
This project provides a simple Flask app for crop prediction based on soil and weather features.

Run locally
Install dependencies: pip install -r requirements.txt
Start the app: python app.py
Open http://127.0.0.1:5000/
Test
Run: python -m unittest discover -s tests